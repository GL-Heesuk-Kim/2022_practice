# 메모


```bash

#git add .
#git rm --cached node_modules
#git status

#git commit -m "00000000"

#git remote add origin git@github.com:GL-Heesuk-Kim/inflearn.git
#git branch -M main
#git push -u origin main

#git@github.com:GL-Heesuk-Kim/inflearn.git

#my local repository ------------(Secure SHell aka.ssh)---------  git hub repository
#ls -a ~/.ssh #exisitence checking


#7
#bodyparser?
#client ---------------(bodyparser)-----------server

#postman??
#=> temporary client for testing .....

#Register Route?
#postman으로 Register Route에 request를 보낸다.

#nodemon?
#=> 서버를 껐다 키지 않아도!

# package.json에서
  "scripts": {
    "start": "node index.js", "_이건 star를 입력하면 node로 실행할게"
    "backend": "nodemon index.js", "_이건 backend를 입력하면 nodemon로 실행할게"

